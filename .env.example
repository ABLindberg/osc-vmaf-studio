# OSC VMAF Studio - Environment Variables
# All configuration can be done through the UI Settings.
# Environment variables are optional and only needed for pre-configuration.

# Server Configuration
PORT=3001

# Frontend Configuration
# API base URL - leave empty for relative URLs (containerized), or set to http://localhost:3001 (development)
VITE_API_BASE_URL=
